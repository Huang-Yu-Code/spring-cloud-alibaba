server:
  port: 8084
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: server:8848
        namespace: public
        file-extension: yaml
      discovery:
        server-addr: server:8848
